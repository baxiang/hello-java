# 2.2 基本数据类型

## 一、Java 数据类型分类

```
Java 数据类型
├── 基本数据类型（8 种）
│   ├── 整数类型：byte, short, int, long
│   ├── 浮点类型：float, double
│   ├── 字符类型：char
│   └── 布尔类型：boolean
└── 引用数据类型
    ├── 类（Class）
    ├── 接口（Interface）
    ├── 数组（Array）
    └── ...
```

## 二、整数类型

### 2.1 四种整数类型

| 类型 | 字节 | 取值范围 | 默认值 |
|------|------|----------|--------|
| byte | 1 | -128 ~ 127 | 0 |
| short | 2 | -32768 ~ 32767 | 0 |
| int | 4 | -2³¹ ~ 2³¹-1 | 0 |
| long | 8 | -2⁶³ ~ 2⁶³-1 | 0L |

### 2.2 使用示例

```java
public class IntegerDemo {
    public static void main(String[] args) {
        // byte：适合存储小整数
        byte age = 18;
        
        // short：较少使用
        short port = 8080;
        
        // int：最常用（默认整数类型）
        int count = 1000;
        
        // long：大整数，末尾加 L
        long population = 7000000000L;
        
        System.out.println("年龄：" + age);
        System.out.println("人口：" + population);
    }
}
```

### 2.3 进制表示

```java
int decimal = 10;      // 十进制
int binary = 0b1010;   // 二进制（Java 7+）
int octal = 012;       // 八进制
int hex = 0xA;         // 十六进制

// 数字分隔符（Java 7+）
int million = 1_000_000;
```

## 三、浮点类型

### 3.1 两种浮点类型

| 类型 | 字节 | 精度 | 默认值 |
|------|------|------|--------|
| float | 4 | 约 7 位有效数字 | 0.0f |
| double | 8 | 约 15 位有效数字 | 0.0 |

### 3.2 使用示例

```java
public class FloatDemo {
    public static void main(String[] args) {
        // float：单精度，末尾加 F
        float price = 99.99f;
        
        // double：双精度（默认浮点类型）
        double pi = 3.1415926535;
        double height = 1.75;
        
        System.out.println("价格：" + price);
        System.out.println("圆周率：" + pi);
    }
}
```

### 3.3 注意事项

```java
// ❌ 错误：小数默认是 double 类型
float f = 3.14;  // 编译错误

// ✅ 正确
float f = 3.14f;  // 加 F 后缀
float f2 = (float) 3.14;  // 强制转换

// 浮点数精度问题
double a = 0.1;
double b = 0.2;
System.out.println(a + b);  // 输出 0.30000000000000004
```

## 四、字符类型

### 4.1 char 类型

| 项目 | 说明 |
|------|------|
| 类型 | char |
| 字节 | 2 |
| 编码 | Unicode |
| 默认值 | '\u0000' |

### 4.2 使用示例

```java
public class CharDemo {
    public static void main(String[] args) {
        // 字符字面量
        char grade = 'A';
        char gender = '男';  // 中文也可以
        
        // 转义字符
        char newline = '\n';
        char tab = '\t';
        char singleQuote = '\'';
        char backslash = '\\';
        
        // Unicode 字符
        char unicode = '\u0041';  // 'A'
        
        // 字符可以参与运算（ASCII/Unicode 值）
        char c = 'A';
        int code = c;  // 65
        
        System.out.println("等级：" + grade);
        System.out.println("'A' 的 ASCII 码：" + code);
    }
}
```

### 4.3 char 与 int 转换

```java
char c1 = 'A';
int i1 = c1;        // 65，自动转换

int i2 = 65;
char c2 = (char) i2;  // 'A'，强制转换

// 字符运算
char c3 = 'A' + 1;  // 'B'
```

## 五、布尔类型

### 5.1 boolean 类型

| 项目 | 说明 |
|------|------|
| 类型 | boolean |
| 取值 | true 或 false |
| 默认值 | false |

### 5.2 使用示例

```java
public class BooleanDemo {
    public static void main(String[] args) {
        // 布尔变量
        boolean isStudent = true;
        boolean isMarried = false;
        
        // 布尔表达式
        int age = 18;
        boolean isAdult = age >= 18;  // true
        
        // 逻辑运算
        boolean result = isStudent && isAdult;  // true
        
        System.out.println("是学生：" + isStudent);
        System.out.println("是成年人：" + isAdult);
    }
}
```

### 5.3 注意事项

```java
// ❌ 错误：boolean 不能与 int 转换
int a = 1;
boolean b = a;  // 编译错误

// ✅ 正确
boolean b = (a == 1);
```

## 六、数据类型选择建议

| 场景 | 推荐类型 |
|------|----------|
| 存储年龄、数量 | int |
| 存储金额 | BigDecimal（不是基本类型） |
| 存储身高、体重 | double |
| 存储性别、等级 | char |
| 存储标志位 | boolean |
| 存储超大整数 | long |

## 七、综合示例

```java
public class StudentInfo {
    public static void main(String[] args) {
        // 基本信息
        String name = "张三";    // 引用类型
        int age = 18;            // int
        char gender = '男';      // char
        
        // 身体信息
        double height = 1.75;    // double
        double weight = 65.5;    // double
        
        // 学习信息
        int grade = 3;           // int
        double score = 95.5;     // double
        char level = 'A';        // char
        boolean isPassed = true; // boolean
        
        System.out.println("=== 学生信息 ===");
        System.out.println("姓名：" + name);
        System.out.println("年龄：" + age);
        System.out.println("性别：" + gender);
        System.out.println("身高：" + height + "米");
        System.out.println("体重：" + weight + "公斤");
        System.out.println("年级：" + grade);
        System.out.println("分数：" + score);
        System.out.println("等级：" + level);
        System.out.println("是否及格：" + isPassed);
    }
}
```

## 八、小结

本节要点：
1. **8 种基本数据类型**：byte, short, int, long, float, double, char, boolean
2. **最常用**：int（整数）、double（小数）、char（字符）、boolean（布尔）
3. **默认值**：数值为 0，char 为'\u0000'，boolean 为 false
4. **字面量后缀**：long 用 L，float 用 F

## 九、练习题

1. 为以下数据选择合适的数据类型：
   - 手机号码
   - 商品价格
   - 是否登录
   - 身份证号
   - 成绩等级（A/B/C/D）

2. 指出以下代码的错误：
```java
float price = 99.99;
int bigNum = 10000000000;
char c = "A";
boolean b = 1;
```

3. 编写程序输出你的基本信息（使用不同的数据类型）

---

[上一节：2.1 变量定义](./02-01-变量定义.md) | [下一节：2.3 类型转换](./02-03-类型转换.md)
