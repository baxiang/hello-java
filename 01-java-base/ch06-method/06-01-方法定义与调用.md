# 6.1 方法的定义与调用

## 一、方法概述

方法（Method）是完成特定功能的代码块，可以被重复调用。

**好处**：
- 提高代码复用性
- 提高代码可维护性
- 使程序结构更清晰

## 二、方法的定义

### 2.1 基本语法

```java
修饰符 返回值类型 方法名 (参数列表) {
    // 方法体
    return 返回值;  // 如果返回值类型不是 void
}
```

### 2.2 方法组成部分

| 部分 | 说明 | 示例 |
|------|------|------|
| 修饰符 | 控制访问权限等 | `public`, `static` |
| 返回值类型 | 方法返回的数据类型 | `int`, `String`, `void` |
| 方法名 | 方法的名称 | `sum`, `printInfo` |
| 参数列表 | 传入的数据 | `int a, int b` |
| 方法体 | 执行的具体代码 | `{ ... }` |
| 返回值 | 返回给调用者的值 | `return result;` |

### 2.3 示例

```java
public class MethodDemo {
    public static void main(String[] args) {
        // 调用方法
        int result = sum(10, 20);
        System.out.println("结果：" + result);
    }
    
    // 定义方法
    public static int sum(int a, int b) {
        int result = a + b;
        return result;
    }
}
```

## 三、方法的调用

### 3.1 调用方式

```java
public class MethodCall {
    public static void main(String[] args) {
        // 方式 1：单独调用（void 方法）
        printStar(5);
        
        // 方式 2：打印调用（有返回值）
        System.out.println(sum(10, 20));
        
        // 方式 3：赋值调用（有返回值）
        int result = sum(10, 20);
        System.out.println("结果：" + result);
    }
    
    public static void printStar(int n) {
        for (int i = 0; i < n; i++) {
            System.out.print("* ");
        }
        System.out.println();
    }
    
    public static int sum(int a, int b) {
        return a + b;
    }
}
```

### 3.2 调用过程

```
调用 sum(10, 20)
    ↓
参数传递：a=10, b=20
    ↓
执行方法体：result = 10 + 20 = 30
    ↓
返回结果：return 30
    ↓
调用处接收返回值
```

## 四、方法的分类

### 4.1 有返回值的方法

```java
public class ReturnMethod {
    public static void main(String[] args) {
        int max = getMax(10, 20);
        System.out.println("最大值：" + max);
    }
    
    public static int getMax(int a, int b) {
        return a > b ? a : b;
    }
}
```

### 4.2 无返回值的方法（void）

```java
public class VoidMethod {
    public static void main(String[] args) {
        printInfo("张三", 18);
    }
    
    public static void printInfo(String name, int age) {
        System.out.println("姓名：" + name);
        System.out.println("年龄：" + age);
    }
}
```

### 4.3 有参数的方法

```java
public class ParameterMethod {
    public static void main(String[] args) {
        int result = add(10, 20);
        System.out.println("和：" + result);
    }
    
    public static int add(int a, int b) {
        return a + b;
    }
}
```

### 4.4 无参数的方法

```java
public class NoParameterMethod {
    public static void main(String[] args) {
        printHello();
    }
    
    public static void printHello() {
        System.out.println("Hello, World!");
    }
}
```

## 五、方法重载

### 5.1 重载概念

方法重载：在同一个类中，方法名相同，参数列表不同。

### 5.2 重载示例

```java
public class OverloadDemo {
    public static void main(String[] args) {
        System.out.println(sum(10, 20));        // 30
        System.out.println(sum(10, 20, 30));    // 60
        System.out.println(sum(10.5, 20.5));    // 31.0
    }
    
    // 两个整数相加
    public static int sum(int a, int b) {
        return a + b;
    }
    
    // 三个整数相加
    public static int sum(int a, int b, int c) {
        return a + b + c;
    }
    
    // 两个小数相加
    public static double sum(double a, double b) {
        return a + b;
    }
}
```

### 5.3 重载判断标准

```java
public class OverloadCheck {
    // ✅ 重载：参数个数不同
    public void method(int a) { }
    public void method(int a, int b) { }
    
    // ✅ 重载：参数类型不同
    public void method(int a) { }
    public void method(double a) { }
    
    // ✅ 重载：参数顺序不同
    public void method(int a, double b) { }
    public void method(double a, int b) { }
    
    // ❌ 不是重载：仅返回值不同
    // public int method(int a) { }
    // public double method(int a) { }
    
    // ❌ 不是重载：仅修饰符不同
    // public void method(int a) { }
    // static void method(int a) { }
}
```

## 六、注意事项

### 6.1 return 语句

```java
public class ReturnDemo {
    public static void main(String[] args) {
        System.out.println(getSign(5));   // 正数
        System.out.println(getSign(-5));  // 负数
        System.out.println(getSign(0));   // 零
    }
    
    public static String getSign(int num) {
        if (num > 0) {
            return "正数";
        } else if (num < 0) {
            return "负数";
        } else {
            return "零";
        }
    }
}
```

### 6.2 void 方法中的 return

```java
public class VoidReturn {
    public static void main(String[] args) {
        printNumber(5);
    }
    
    public static void printNumber(int n) {
        if (n < 0) {
            return;  // void 方法可以省略 return
        }
        System.out.println(n);
    }
}
```

### 6.3 参数传递

```java
public class ParameterPass {
    public static void main(String[] args) {
        int x = 10;
        change(x);
        System.out.println(x);  // 10，基本类型值传递
        
        int[] arr = {1, 2, 3};
        changeArray(arr);
        System.out.println(arr[0]);  // 100，引用类型地址传递
    }
    
    public static void change(int n) {
        n = 100;
    }
    
    public static void changeArray(int[] arr) {
        arr[0] = 100;
    }
}
```

## 七、综合示例

```java
public class MethodExample {
    public static void main(String[] args) {
        // 1. 基本调用
        int sum = add(10, 20);
        System.out.println("和：" + sum);
        
        // 2. 重载调用
        System.out.println("三数和：" + add(10, 20, 30));
        System.out.println("小数和：" + add(1.5, 2.5));
        
        // 3. void 方法
        printLine(20);
        
        // 4. 组合调用
        int max = getMax(10, 20);
        int min = getMin(10, 20);
        System.out.println("最大值：" + max + ", 最小值：" + min);
    }
    
    // 两个整数相加
    public static int add(int a, int b) {
        return a + b;
    }
    
    // 三个整数相加
    public static int add(int a, int b, int c) {
        return a + b + c;
    }
    
    // 两个小数相加
    public static double add(double a, double b) {
        return a + b;
    }
    
    // 打印分隔线
    public static void printLine(int n) {
        for (int i = 0; i < n; i++) {
            System.out.print("-");
        }
        System.out.println();
    }
    
    // 获取最大值
    public static int getMax(int a, int b) {
        return a > b ? a : b;
    }
    
    // 获取最小值
    public static int getMin(int a, int b) {
        return a < b ? a : b;
    }
}
```

## 八、小结

本节要点：
1. **方法定义**：修饰符 + 返回值类型 + 方法名 + 参数列表 + 方法体
2. **方法调用**：单独调用、打印调用、赋值调用
3. **方法分类**：有/无返回值，有/无参数
4. **方法重载**：方法名相同，参数列表不同
5. **参数传递**：基本类型值传递，引用类型地址传递

---

[下一节：6.2 方法的重载](./06-02-方法重载.md)
