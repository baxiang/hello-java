# 77.5 日志规范：如何正确记录日志？

## 一、先问一个问题

**为什么要规范日志？**

### 生活例子：飞机黑匣子

```
没有日志
└── 出问题不知道原因

有日志
├── 记录关键信息
└── 快速定位问题
```

**日志作用**：
- 问题排查
- 运行监控
- 审计追溯

## 二、日志级别

### 级别定义

```
ERROR → 错误（系统异常）
WARN  → 警告（可恢复异常）
INFO  → 信息（正常运行）
DEBUG → 调试（开发调试）
TRACE → 追踪（详细追踪）
```

### 使用场景

```java
// ERROR：系统异常
log.error("数据库连接失败", e);

// WARN：警告
log.warn("用户 {} 登录失败，剩余尝试次数：{}", username, retryCount);

// INFO：重要信息
log.info("用户 {} 登录成功", username);

// DEBUG：调试信息
log.debug("查询 SQL: {}", sql);

// TRACE：详细追踪
log.trace("方法入口：userId={}", userId);
```

## 三、日志规范

### 正确示例

```java
// ✅ 好的日志
log.info("创建订单成功，orderId={}, userId={}", orderId, userId);

log.error("数据库连接失败，url={}", dbUrl, e);

log.warn("库存不足，productId={}, currentStock={}, required={}", 
    productId, currentStock, required);
```

### 错误示例

```java
// ❌ 坏的日志
log.info("成功");  // 太简单

log.error("出错了" + e.getMessage());  // 丢失堆栈

log.debug("用户=" + user + ", 订单=" + order);  // 字符串拼接
```

## 四、Logback 配置

### 配置文件

```xml
<configuration>
    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- 文件输出 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/app.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/app.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- 错误日志 -->
    <appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/error.log</file>
        <filter class="ch.qos.logback.classic.filter.LevelFilter">
            <level>ERROR</level>
            <onMatch>ACCEPT</onMatch>
            <onMismatch>DENY</onMismatch>
        </filter>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>logs/error.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
    
    <!-- 根日志级别 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
        <appender-ref ref="ERROR_FILE"/>
    </root>
</configuration>
```

## 五、日志排查

### 快速定位

```bash
# 查看错误日志
tail -f logs/error.log

# 搜索关键字
grep "userId=123" logs/app.log

# 查看最近 100 行
tail -100 logs/app.log
```

### 链路追踪

```java
// 添加 traceId
MDC.put("traceId", UUID.randomUUID().toString());

try {
    // 业务逻辑
    log.info("处理订单");
} finally {
    MDC.clear();
}
```

## 六、小结

| 级别 | 用途 | 示例 |
|------|------|------|
| ERROR | 错误 | 系统异常 |
| WARN | 警告 | 可恢复异常 |
| INFO | 信息 | 正常运行 |
| DEBUG | 调试 | 开发调试 |
| TRACE | 追踪 | 详细追踪 |

**核心要点**：
- 日志级别选择
- 日志格式规范
- 日志配置管理

---

[上一节：77.4 单元测试](./77-04-单元测试/14-09-单元测试.md)

## 项目通用技能完成！

通过这 5 节，你完成了：
1. ✅ 代码规范（命名、结构、注释）
2. ✅ Git 工作流（分支管理）
3. ✅ 接口文档（Swagger/Knife4j）
4. ✅ 单元测试（JUnit/Mockito）
5. ✅ 日志规范（级别、配置、排查）

---

[上一节：77.4 单元测试](./77-04-单元测试/14-09-单元测试.md)
