# 76.3 内容管理：如何管理文章？

## 一、先问一个问题

**文章如何管理？**

### 生活例子：报社编辑

```
编辑流程
├── 写稿
├── 审核
├── 排版
└── 发布
```

**内容管理**：
- 文章 CRUD
- 富文本编辑
- 审核流程

## 二、数据库设计

### 文章表

```sql
CREATE TABLE `article` (
    `id` BIGINT PRIMARY KEY AUTO_INCREMENT,
    `title` VARCHAR(200) NOT NULL COMMENT '标题',
    `content` LONGTEXT COMMENT '内容',
    `category_id` BIGINT COMMENT '栏目 ID',
    `author` VARCHAR(50) COMMENT '作者',
    `status` TINYINT DEFAULT 0 COMMENT '状态（0 草稿/1 待审/2 已发布）',
    `view_count` INT DEFAULT 0 COMMENT '阅读量',
    `create_time` DATETIME DEFAULT CURRENT_TIMESTAMP,
    `publish_time` DATETIME COMMENT '发布时间'
) COMMENT='文章表';
```

## 三、文章管理

### 发布文章

```java
@PostMapping("/articles")
public Result<Article> publishArticle(@RequestBody ArticleDTO dto) {
    Article article = new Article();
    article.setTitle(dto.getTitle());
    article.setContent(dto.getContent());
    article.setCategoryId(dto.getCategoryId());
    article.setStatus(2);  // 已发布
    article.setPublishTime(LocalDateTime.now());
    
    articleMapper.insert(article);
    return Result.success(article);
}
```

### 富文本编辑

```html
<!-- 引入富文本编辑器 -->
<script src="https://cdn.ckeditor.com/ckeditor5/35.0.1/classic/ckeditor.js"></script>

<textarea id="editor"></textarea>

<script>
    ClassicEditor
        .create(document.querySelector('#editor'))
        .catch(error => console.error(error));
</script>
```

## 四、小结

| 功能 | 实现方式 | 说明 |
|------|----------|------|
| 文章发布 | 插入数据库 | 设置状态 |
| 富文本编辑 | CKEditor | 可视化编辑 |
| 文章审核 | 状态管理 | 草稿/待审/已发布 |

**核心要点**：
- 文章内容存储
- 富文本编辑器
- 状态管理

---

[上一节：76.2 栏目管理](./76-02-栏目管理/14-08-栏目管理.md) | 
[下一节：76.4 模板引擎](./76-04-模板引擎/14-08-模板引擎.md)
